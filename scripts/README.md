# scripts — 工具脚本

## 职责

- 独立可运行的开发/运维脚本，不依赖服务启动：
  - init_milvus.py：创建 collection 与索引（向量库 #5）；
  - test_query.py：命令行单测一条问题（检索 #6 / 测试 #9）；
  - ingest.py：预建知识库批量抓取入库（爬虫 #2 + 向量库 #5 协作）。

## 约定

- 脚本参数与配置从 config/ 读取，不硬编码；
- 脚本需能在本地环境与 Docker 容器内两种环境运行。
